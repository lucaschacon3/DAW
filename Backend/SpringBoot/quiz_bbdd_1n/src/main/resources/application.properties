# Nombre de la aplicación
spring.application.name=quiz_bbdd_1n

# Configuración de la base de datos
spring.datasource.url=jdbc:mysql://localhost:3306/dwes2
spring.datasource.username=daw2
spring.datasource.password=LaElipa
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de Hibernate (JPA)
# Cambia a 'update' para mantener las tablas actualizadas automáticamente
spring.jpa.hibernate.ddl-auto=update  
# Muestra las consultas SQL generadas por Hibernate
spring.jpa.show-sql=true               
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Configuración de Thymeleaf
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML

# Perfil activo
spring.profiles.active=dev
